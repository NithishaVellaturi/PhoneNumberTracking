# Local Vite development can keep this as /api because vite.config.ts proxies to localhost:8080.
# For Netlify production, set this in the Netlify UI to your deployed backend URL, for example:
# VITE_API_BASE_URL=https://your-backend-domain.com/api
VITE_API_BASE_URL=/api
